#include <stdio.h>

int main()
{

    // For 0.1

    // Data type used is float

    float s = 0;
    int i = 1;

    while (i <= 1000000)
    {
        s = s + 0.1;
        i++;
    }

    printf("%f", s);

    // Data type used is double

    double s = 0;
    int i = 1;

    while (i <= 1000000)
    {
        s = s + 0.1;
        i++;
    }

    printf("%lf", s);

    // For 1

    // Data type used is float

    float s = 0;
    int i = 1;

    while (i <= 1000000)
    {
        s = s + 0.1;
        i++;
    }

    printf("%f", s);

    // Data type used is double

    double s = 0;
    int i = 1;

    while (i <= 1000000)
    {
        s = s + 0.1;
        i++;
    }

    printf("%lf", s);
}